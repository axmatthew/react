import React from 'react';
import { shallow } from 'enzyme';
import DataTable from '../../../src/components/data-table/data-table.jsx';

describe('<DataTable />', () => {

  it('should render children in tbody', () => {});
  it('should render a list of th in thead based on fieldConfigs', () => {});
  it('should render items in the specific order provided', () => {});
  it('should still render xxx even if yyy is not provided', ()  => {});

});
